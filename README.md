# 📱 RemotPhone — Удалённый доступ к Android с ПК

Приложение для удалённого управления Android-телефоном с компьютера через браузер.

---

## 🏗️ Архитектура

```
┌──────────┐     WebSocket      ┌──────────┐     WebSocket      ┌──────────┐
│  Android  │ ◄──────────────► │  Сервер   │ ◄──────────────► │  Браузер  │
│  (агент)  │   экран, данные   │  (relay)  │   управление      │   (ПК)   │
└──────────┘                    └──────────┘                    └──────────┘
```

**3 компонента:**
1. **Сервер** (Node.js) — пересылает сообщения между телефоном и ПК
2. **Android-приложение** — захватывает экран, принимает команды
3. **Веб-клиент** — интерфейс управления в браузере на ПК

---

## 🚀 Быстрый старт

### Шаг 1: Запуск сервера

Сервер запускается на любом компьютере в вашей сети (или на VPS для доступа через интернет).

```bash
# Перейти в папку сервера
cd server

# Установить зависимости
npm install

# Запустить
npm start
```

Сервер запустится на порту **3000**. Вы увидите:
```
╔══════════════════════════════════════════════╗
║          🔗 RemotPhone Server                ║
║  Web Client:  http://localhost:3000          ║
║  WebSocket:   ws://localhost:3000            ║
╚══════════════════════════════════════════════╝
```

**⚠️ Узнайте IP вашего компьютера:**
- Windows: `ipconfig` → ищите IPv4 (напр. `192.168.1.100`)
- macOS/Linux: `ifconfig` или `ip addr`

### Шаг 2: Сборка Android-приложения

```bash
# Откройте папку android-app в Android Studio
# File → Open → выберите папку android-app

# ИЛИ соберите из командной строки:
cd android-app
./gradlew assembleDebug

# APK будет в: app/build/outputs/apk/debug/app-debug.apk
```

Установите APK на телефон.

### Шаг 3: Настройка телефона

1. **Откройте RemotPhone** на телефоне
2. **Введите адрес сервера**: `ws://192.168.1.100:3000` (замените IP на ваш)
3. **Нажмите «Подключиться»** — появится 6-значный код
4. **Выдайте разрешения** (кнопки внизу экрана):
   - ⚙️ **Accessibility Service** — для удалённого управления (тапы, свайпы)
   - 🔔 **Уведомления** — для пересылки уведомлений на ПК
   - 🔋 **Батарея** — чтобы Android не убивал приложение в фоне
5. **Нажмите «Начать трансляцию экрана»** и подтвердите

### Шаг 4: Подключение с ПК

1. Откройте браузер на ПК: `http://192.168.1.100:3000`
2. Введите **6-значный код** с телефона
3. Нажмите **«Подключиться»**
4. Готово! Вы видите экран телефона и можете управлять им

---

## 🎮 Возможности

| Функция | Описание |
|---------|----------|
| 📺 **Экран** | Трансляция экрана телефона в реальном времени |
| 👆 **Тапы** | Кликайте мышкой на экране — тап на телефоне |
| 👆 **Свайпы** | Зажмите и проведите мышкой — свайп на телефоне |
| ⌨️ **Клавиатура** | Набирайте текст с клавиатуры ПК |
| 🔔 **Уведомления** | Все уведомления телефона видны на ПК |
| 💬 **SMS** | Читайте и отправляйте SMS с ПК |
| 📂 **Файлы** | Просмотр файлов на телефоне |
| 📱 **Приложения** | Список установленных приложений, запуск с ПК |
| 📋 **Буфер обмена** | Копирование текста между ПК и телефоном |
| 🔊 **Громкость** | Управление громкостью с ПК |
| 🔙 **Навигация** | Кнопки Назад / Домой / Недавние |

---

## 🌐 Доступ через интернет

Для доступа из любой точки мира есть несколько вариантов:

### Вариант A: VPS сервер
1. Арендуйте VPS (DigitalOcean, Hetzner, и т.д.)
2. Установите Node.js
3. Скопируйте папки `server/` и `web-client/` на VPS
4. Запустите: `npm start`
5. Используйте `ws://YOUR_VPS_IP:3000`

### Вариант B: ngrok (быстро для тестов)
```bash
# Установите ngrok: https://ngrok.com
ngrok http 3000
```
Используйте URL от ngrok (напр. `wss://abc123.ngrok.io`)

### Вариант C: Tailscale / ZeroTier
Создайте VPN-сеть между устройствами — сервер будет доступен по внутреннему IP.

---

## 🔒 Безопасность

- Соединение защищено **6-значным кодом** (меняется при каждом подключении)
- Для production рекомендуется:
  - Использовать **WSS (WebSocket Secure)** с SSL-сертификатом
  - Добавить аутентификацию по паролю
  - Ограничить доступ по IP

### Добавление SSL (nginx пример):
```nginx
server {
    listen 443 ssl;
    server_name your-domain.com;
    
    ssl_certificate /path/to/cert.pem;
    ssl_certificate_key /path/to/key.pem;
    
    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
```

---

## 🛠️ Решение проблем

### Телефон не подключается к серверу
- Убедитесь что телефон и сервер в одной сети
- Проверьте что порт 3000 не блокируется файрволом
- Проверьте правильность адреса (`ws://` не `http://`)

### Экран не транслируется
- Нажмите «Начать трансляцию» на телефоне и подтвердите системный диалог
- Если качество плохое — уменьшите `frameQuality` в `RemoteService.kt`

### Тапы/свайпы не работают
- Включите **Accessibility Service** в настройках Android
- Путь: Настройки → Специальные возможности → RemotPhone → Включить

### Уведомления не приходят
- Включите **доступ к уведомлениям** в настройках
- Путь: Настройки → Приложения → Спец. доступ → Уведомления → RemotPhone

### Android убивает приложение в фоне
- Отключите **оптимизацию батареи** для RemotPhone
- На Xiaomi/Samsung/Huawei: отдельные настройки автозапуска

---

## 📁 Структура проекта

```
remote-phone/
├── server/                    # Node.js relay сервер
│   ├── server.js              # Основной сервер
│   └── package.json
├── web-client/                # Веб-интерфейс для ПК
│   └── index.html             # Единый HTML файл
├── android-app/               # Android приложение
│   ├── build.gradle
│   ├── settings.gradle
│   └── app/
│       ├── build.gradle
│       ├── proguard-rules.pro
│       └── src/main/
│           ├── AndroidManifest.xml
│           ├── java/com/remotphone/agent/
│           │   ├── MainActivity.kt           # UI
│           │   ├── RemoteService.kt          # Сервис: WS, экран, данные
│           │   ├── RemoteAccessibilityService.kt  # Тапы, свайпы
│           │   └── NotificationListener.kt   # Уведомления
│           └── res/
│               ├── layout/activity_main.xml
│               ├── values/strings.xml
│               ├── values/themes.xml
│               ├── drawable/card_bg.xml
│               ├── drawable/dot_online.xml
│               ├── drawable/dot_offline.xml
│               └── xml/
│                   ├── accessibility_config.xml
│                   └── network_security_config.xml
└── README.md
```

---

## ⚡ Настройка производительности

В `RemoteService.kt` можно изменить:

```kotlin
private var frameQuality = 40   // JPEG качество: 10 (быстро, плохо) — 90 (медленно, чётко)
private var frameInterval = 100L // Интервал между кадрами в мс (100 = ~10 fps)
```

Для медленного интернета: `quality=20, interval=300`
Для локальной сети: `quality=60, interval=50`

Также эти параметры можно менять с ПК через меню настроек (если добавить UI).
